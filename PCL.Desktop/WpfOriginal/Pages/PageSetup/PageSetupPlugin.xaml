<local:MyPageRight
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:PCL"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="PCL.PageSetupPlugin"
    PanScroll="{Binding ElementName=PanBack, Mode=OneWay}">
    <local:MyScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" x:Name="PanBack">
        <StackPanel x:Name="PanMain" Margin="25,25,25,10">
            <local:MyCard Margin="0,0,0,15" Title="{DynamicResource Setup.Plugin.Title}">
                <StackPanel Margin="25,40,25,20">
                    <TextBlock Text="{DynamicResource Setup.Plugin.ComingSoon}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ColorBrush1}"
                               Margin="0,0,0,10" />
                    <TextBlock Text="{DynamicResource Setup.Plugin.Description}"
                               FontSize="13"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource ColorBrush2}" />
                </StackPanel>
            </local:MyCard>
        </StackPanel>
    </local:MyScrollViewer>
</local:MyPageRight>

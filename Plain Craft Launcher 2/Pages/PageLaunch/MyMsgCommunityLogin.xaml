<Grid x:Class="PCL.MyMsgCommunityLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:PCL"
      xmlns:controls="clr-namespace:PCL.Core.UI.Controls;assembly=PCL.Core"
      RenderTransformOrigin="0.5,0.5"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
      Margin="24">
    <Grid.RenderTransform>
        <TransformGroup>
            <RotateTransform x:Name="TransformRotate" Angle="-2" />
            <TranslateTransform x:Name="TransformPos" X="0" Y="32" />
        </TransformGroup>
    </Grid.RenderTransform>

    <controls:BlurBorder x:Name="PanBorder"
                         MinWidth="320"
                         MaxWidth="560"
                         CornerRadius="10"
                         Background="{DynamicResource ColorBrushBackground}">
        <controls:BlurBorder.Effect>
            <DropShadowEffect Color="{DynamicResource ColorObjectMsgBoxShadow}"
                              BlurRadius="24"
                              ShadowDepth="5"
                              RenderingBias="Performance"
                              Opacity="0.85" />
        </controls:BlurBorder.Effect>

        <Grid Margin="22,18,22,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="2" />
                <RowDefinition Height="16" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="LabTitle"
                               FontSize="23"
                               Margin="4,0,16,0"
                               Foreground="{DynamicResource ColorBrush2}"
                               FontWeight="SemiBold" />
                    <TextBlock x:Name="LabSubtitle"
                               Margin="4,6,16,0"
                               Opacity="0.74"
                               TextWrapping="Wrap" />
                </StackPanel>

                <local:MyIconButton Grid.Column="1"
                                    x:Name="BtnClose"
                                    Width="28"
                                    Height="28"
                                    Theme="Black"
                                    SvgIcon="lucide/x" />
            </Grid>

            <Rectangle Grid.Row="1"
                       Height="2"
                       Fill="{Binding Foreground, ElementName=LabTitle}" />

            <ContentPresenter Grid.Row="3"
                              x:Name="PanContent" />
        </Grid>
    </controls:BlurBorder>
</Grid>
